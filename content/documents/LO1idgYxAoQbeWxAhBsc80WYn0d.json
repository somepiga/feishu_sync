{
  "id": "LO1idgYxAoQbeWxAhBsc80WYn0d",
  "title": "5 stream 方案",
  "updated_at": "2026-03-05T14:22:15+00:00",
  "raw_content": "5 stream 方案\n背景\n训练中使用 unsafe stream 做计算 / 通信 / offload 时，GPU 的 CP 队列 是核心瓶颈：\nGPU 的 event wait 操作会 占用 CP 队列资源 ，而 4 个基础 stream 扩展到 5 个后，offload 的 SDMA stream（stream4）需要和计算 stream（stream0）做同步，直接用 GPU 原生的 event wait 会耗尽 CP、导致 stream 间互相阻塞；\nGPU 的 SDMA 仅负责数据搬运（H2D/D2H/D2D），但 缺乏独立的任务调度和同步能力 ，需要外部逻辑做 “等待计算完成→触发 DMA→通知计算流” 的调度。\n方案设计\n使用5个stream，其中1个stream既有SDMA，也有CP；3个stream只有CP，1个stream只有SDMA\nstream0占用CP queue+SDMA queue，做SPC计算+Dataloader\nstream1占用CP queue，做TP/CP通信\nstream2，stream3占用CP queue，做PP/DP通信\nstream4 占用SDMA queue，使用SDMA做offload\nimage2026-1-30_16-27-29.png\n\n需要解决的问题： Offload(stream4)需要event wait stream0上的计算结果才能做offload，但event wait会占用CP（单纯record event不会占用CP）。所以此处得使用其他方案绕开event wait\n解决方法\n使用一个CPU单线程模拟GPU的stream功能，命名为：DMAStream，DMAStream封装一个只可以执行DMA copy的gpu stream和一个cpu thread。\nDMAStream支持通过封装的stream跑DMA Copy任务。同步GPU任务。\nDMAStream支持set一个DMAEvent的状态。CPU任务。\nDMAStream支持block wait一个DMAEvent的状态。CPU任务。\n上面三种操作都是以一个thread task的形式提交到thread上排队执行。\nDMAStream支持record gpu Event，通过封装的gpu stream实现，此操作不占用CP Queue。\n定义DMAEvent，模拟gpu的Event功能。\n在DMA Stream上Record DMAEvent，DMAStream按顺序去set DMAEvent状态。\n在GPU Stream上Record DMAEvent，通过GPU Stream的MemOp操作set DMAEvent的状态。\n在DMA Stream上Wait DMAEvent，DMAStream通过block wait DMAEvent实现。\n在GPU Stream上Wait DMAEvent，通过GPU Stream的MemOp操作block Wait DMAEvent。\n在cpu上wait DMAEvent状态。\n通过以上两个class和对应的API可以实现只支持DMA操作的一个DMAStream，并可与其他正常的GPU Stream同步，DMAStream上的任务顺序通过单thread保证；wait操作是通过CPU实现的；record操作不占用CP Queue资源。GPU Stream通过mem op操作和DMAStream同步，实现record和wait功能。\n",
  "render_html": "<h2>背景</h2><p>训练中使用 <strong>unsafe stream </strong>做计算 / 通信 / offload 时，GPU 的 <strong>CP 队列 </strong>是核心瓶颈：</p><ul><li>GPU 的 event wait 操作会 <strong>占用 CP 队列资源 </strong>，而 4 个基础 stream 扩展到 5 个后，offload 的 SDMA stream（stream4）需要和计算 stream（stream0）做同步，直接用 GPU 原生的 event wait 会耗尽 CP、导致 stream 间互相阻塞；</li><li>GPU 的 SDMA 仅负责数据搬运（H2D/D2H/D2D），但 <strong>缺乏独立的任务调度和同步能力 </strong>，需要外部逻辑做 “等待计算完成→触发 DMA→通知计算流” 的调度。</li></ul><h2>方案设计</h2><p>使用5个stream，其中1个stream既有SDMA，也有CP；3个stream只有CP，1个stream只有SDMA</p><ol><li>stream0占用CP queue+SDMA queue，做SPC计算+Dataloader</li><li>stream1占用CP queue，做TP/CP通信</li><li>stream2，stream3占用CP queue，做PP/DP通信</li><li>stream4 占用SDMA queue，使用SDMA做offload</li></ol><figure class=\"doc-image\"><img src=\"content/media/K7ugbP3w7oGaguxQZQZcxqN5nEY.png\" alt=\"image\" loading=\"lazy\" width=\"770\" height=\"381\"></figure><p><strong>需要解决的问题： </strong>Offload(stream4)需要event wait stream0上的计算结果才能做offload，但event wait会占用CP（单纯record event不会占用CP）。所以此处得使用其他方案绕开event wait</p><h2>解决方法</h2><ol><li>使用一个CPU单线程模拟GPU的stream功能，命名为：DMAStream，DMAStream封装一个只可以执行DMA copy的gpu stream和一个cpu thread。<ol><li>DMAStream支持通过封装的stream跑DMA Copy任务。同步GPU任务。</li><li>DMAStream支持set一个DMAEvent的状态。CPU任务。</li><li>DMAStream支持block wait一个DMAEvent的状态。CPU任务。</li><li>上面三种操作都是以一个thread task的形式提交到thread上排队执行。</li><li>DMAStream支持record gpu Event，通过封装的gpu stream实现，此操作不占用CP Queue。</li></ol></li><li>定义DMAEvent，模拟gpu的Event功能。<ol><li>在DMA Stream上Record DMAEvent，DMAStream按顺序去set DMAEvent状态。</li><li>在GPU Stream上Record DMAEvent，通过GPU Stream的MemOp操作set DMAEvent的状态。</li><li>在DMA Stream上Wait DMAEvent，DMAStream通过block wait DMAEvent实现。</li><li>在GPU Stream上Wait DMAEvent，通过GPU Stream的MemOp操作block Wait DMAEvent。</li><li>在cpu上wait DMAEvent状态。</li></ol></li><li>通过以上两个class和对应的API可以实现只支持DMA操作的一个DMAStream，并可与其他正常的GPU Stream同步，DMAStream上的任务顺序通过单thread保证；wait操作是通过CPU实现的；record操作不占用CP Queue资源。GPU Stream通过mem op操作和DMAStream同步，实现record和wait功能。</li></ol>",
  "assets": [
    {
      "token": "K7ugbP3w7oGaguxQZQZcxqN5nEY",
      "name": "image2026-1-30_16-27-29.png",
      "path": "content/media/K7ugbP3w7oGaguxQZQZcxqN5nEY.png",
      "content_type": "image/png",
      "type": "image"
    }
  ],
  "blocks": [],
  "node_token": "LzTLwxnsFiU3xWkbnRycG3QwnXb",
  "parent_node_token": "B9ZxwFRfSiRhDQkYzWPcXY8Pncv",
  "wiki_token": "Wh1CwndtIiDWMzk8YutcIlZtn4d",
  "space_id": "7613776537935514838",
  "type": "docx",
  "sync_format_version": 4
}