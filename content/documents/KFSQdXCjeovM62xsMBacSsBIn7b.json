{
  "id": "KFSQdXCjeovM62xsMBacSsBIn7b",
  "title": "显存优化",
  "updated_at": "2026-03-05T14:22:16+00:00",
  "raw_content": "显存优化\n1.  SBP架构下显存优化方案\n这个方案的核心思想是通过使用SB（Split-Batch）存储方式来优化显存使用，特别是针对大模型训练中的三个主要显存开销： weight（权重） 、 gradient（梯度） 和 optimizer state（优化器状态） 。具体措施包括：\n基本原则 ：\n所有的weight、grad和optimizer state都用SB存储，并在初始化时固定其属性（如shape、dtype、layout、sbp等）。\n通过flatten tensor方案支持reorder消除，减少通信和分布式策略下的reorder操作。\n对于Zero策略，weight等会被flatten为flatten tensor，再被partition到多个GPU，使用时需要all gather恢复。\n对于Megatron DeepSpeed，一般也会在optimize中flatten Weight和grad tensor，使用时再narrow出来。\n对于BB初始化的Tensor，可以存储为SB，使用时copy到一个BB Tensor使用。\n难点 ：\n后端有sbp属性，但无法感知图信息，同一算子在图中不同节点需要使用不同的sbp属性。\n例如，Linear算子在前向和反向传播中可能需要不同的sbp属性。\n2.  方案1：前端指定SBP属性\n在前端创建模型时，给Weight指定SBP属性。具体方法包括：\nTensor.sbp ：在Tensor对象中添加一个属性来指定SBP。\n3.  方案2：使用Hook自动创建SBP\n通过Hook方式自动为weight创建sbp。在Model Provider里对最终Model做处理，加入初始化Meta信息。具体方法包括：\nTensor.sbp ：在Tensor对象中添加一个属性来指定SBP。\n4.  融合算子\n对于MLP、Attention等融合算子，可以直接指定weight的sbp。但有些算子融合不彻底（如ScaleDotAttention），需要特别处理。例如：\nMLP ：\n输入X为BB类型。\nWeight为SB类型，具体sbp属性根据算子需求指定。\n通过Matmul、Silu等操作，最终输出为BB类型。\n5.  DeepSpeed Zero策略下的显存优化\nDeepSpeed Zero策略的核心是在两个die上再做一次Zero3拆分。具体措施包括：\nPartition ：根据Tensor的sbp属性获取device size，然后进行partition。\n如果sbp=sb，则分别partition两个region到多个rank。\n如果sbp=bb，则只取一个region partition到多个rank的两个region。\nGather ：根据Tensor的sbp和device size创建Tensor。\n如果sbp=sb，则两个region的数据各自直接cat。\n如果sbp=bb，则需要特殊concat方法。\nReduce ：在Megatron框架下，需要优化weight的SBP策略和optimizer的flatten操作。\n6.  Megatron框架下的显存优化\nMegatron显存优化需要考虑两个方面：\nWeight SBP策略优化 ：通过修改定制的Model类（如RowParallelLinear、ColumnParallelLinear等），对其中的Weight Tensor加入sbp属性。\nOptimizer优化 ：在bf16 optimizer类内，将基于buffer的flatten改为基于SBP的flatten，并调整narrow操作。\n",
  "render_html": "<h4>1.  <strong>SBP架构下显存优化方案</strong></h4><p>这个方案的核心思想是通过使用SB（Split-Batch）存储方式来优化显存使用，特别是针对大模型训练中的三个主要显存开销： <strong>weight（权重） </strong>、 <strong>gradient（梯度） </strong>和 <strong>optimizer state（优化器状态） </strong>。具体措施包括：</p><ul><li><strong>基本原则 </strong>：<ul><li>所有的weight、grad和optimizer state都用SB存储，并在初始化时固定其属性（如shape、dtype、layout、sbp等）。</li><li>通过flatten tensor方案支持reorder消除，减少通信和分布式策略下的reorder操作。</li><li>对于Zero策略，weight等会被flatten为flatten tensor，再被partition到多个GPU，使用时需要all gather恢复。</li><li>对于Megatron DeepSpeed，一般也会在optimize中flatten Weight和grad tensor，使用时再narrow出来。</li><li>对于BB初始化的Tensor，可以存储为SB，使用时copy到一个BB Tensor使用。</li></ul></li><li><strong>难点 </strong>：<ul><li>后端有sbp属性，但无法感知图信息，同一算子在图中不同节点需要使用不同的sbp属性。</li><li>例如，Linear算子在前向和反向传播中可能需要不同的sbp属性。</li></ul></li></ul><h4>2.  <strong>方案1：前端指定SBP属性</strong></h4><p>在前端创建模型时，给Weight指定SBP属性。具体方法包括：</p><ul><li><strong>Tensor.sbp </strong>：在Tensor对象中添加一个属性来指定SBP。</li></ul><h4>3.  <strong>方案2：使用Hook自动创建SBP</strong></h4><p>通过Hook方式自动为weight创建sbp。在Model Provider里对最终Model做处理，加入初始化Meta信息。具体方法包括：</p><ul><li><strong>Tensor.sbp </strong>：在Tensor对象中添加一个属性来指定SBP。</li></ul><h4>4.  <strong>融合算子</strong></h4><p>对于MLP、Attention等融合算子，可以直接指定weight的sbp。但有些算子融合不彻底（如ScaleDotAttention），需要特别处理。例如：</p><ul><li><strong>MLP </strong>：<ul><li>输入X为BB类型。</li><li>Weight为SB类型，具体sbp属性根据算子需求指定。</li><li>通过Matmul、Silu等操作，最终输出为BB类型。</li></ul></li></ul><h4>5.  <strong>DeepSpeed Zero策略下的显存优化</strong></h4><p>DeepSpeed Zero策略的核心是在两个die上再做一次Zero3拆分。具体措施包括：</p><ul><li><strong>Partition </strong>：根据Tensor的sbp属性获取device size，然后进行partition。<ul><li>如果sbp=sb，则分别partition两个region到多个rank。</li><li>如果sbp=bb，则只取一个region partition到多个rank的两个region。</li></ul></li><li><strong>Gather </strong>：根据Tensor的sbp和device size创建Tensor。<ul><li>如果sbp=sb，则两个region的数据各自直接cat。</li><li>如果sbp=bb，则需要特殊concat方法。</li></ul></li><li><strong>Reduce </strong>：在Megatron框架下，需要优化weight的SBP策略和optimizer的flatten操作。</li></ul><h4>6.  <strong>Megatron框架下的显存优化</strong></h4><p>Megatron显存优化需要考虑两个方面：</p><ul><li><strong>Weight SBP策略优化 </strong>：通过修改定制的Model类（如RowParallelLinear、ColumnParallelLinear等），对其中的Weight Tensor加入sbp属性。</li><li><strong>Optimizer优化 </strong>：在bf16 optimizer类内，将基于buffer的flatten改为基于SBP的flatten，并调整narrow操作。</li></ul>",
  "assets": [],
  "blocks": [],
  "node_token": "GdPLwaWzGiyB4WkCuqbcX5Dvnp7",
  "parent_node_token": "HErWwdR1WizUZAkEDLxc8foInlf",
  "wiki_token": "Wh1CwndtIiDWMzk8YutcIlZtn4d",
  "space_id": "7613776537935514838",
  "type": "docx",
  "sync_format_version": 4
}